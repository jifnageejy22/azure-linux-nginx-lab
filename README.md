
# Azure Linux VM + Nginx Lab

## Objective
Deploy a Linux VM in Azure and host a web server.

## What I Did
- Created Ubuntu VM in Azure
- Connected via SSH
- Installed Nginx
- Opened port 80
- Accessed web server via browser

## Commands Used
sudo apt update  
sudo apt install nginx -y  

## Outcome
Successfully hosted Nginx on Azure VM.
